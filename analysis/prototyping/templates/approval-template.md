# Stage 8 Approval — Wireframes

- Date: YYYY-MM-DD
- Approved by: project owner
- Approved export set: `export_set_version: <value from screen-manifest.json>`
- Stage 7 control pass with a dry-pass result: `analysis/reviews/stage-07-pass-NNN.md`

## Owner remarks resolved before approval

| Remark | Resolution | Returned to Stage 6? |
|---|---|---|
| <remark> | <what changed> | yes/no |

## Statement

The owner approves the wireframe set identified above as the visual baseline
for Stage 9 SDD. Any later SDD or implementation change to the UI returns to
Stage 6 and repeats Stage 7 control and this approval for the changed scope.

An approval that does not name the exact `export_set_version` of
`screen-manifest.json` does not exist.
